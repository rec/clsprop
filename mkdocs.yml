site_name: '🏫: `clsprop`: A decorator for class properties 🏫'

theme:
  name: material

  palette:
    primary:
      'indigo'

  font:
    text:
      Roboto
    code:
      Roboto Mono

extra_css: [extra.css]

plugins:
    - mkdocstrings
